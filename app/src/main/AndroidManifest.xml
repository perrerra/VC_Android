<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobile.veloconnecte.vcandroid">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.BikeListActivity" />
        <activity android:name=".activities.BikeDetailActivity" />
        <activity android:name=".activities.RideListActivity" />
        <activity android:name=".activities.RideDetailActivity" />
        <activity android:name=".activities.RideCreateActivity" />
        <activity android:name=".activities.RideCurrentActivity"></activity>
    </application>

</manifest>